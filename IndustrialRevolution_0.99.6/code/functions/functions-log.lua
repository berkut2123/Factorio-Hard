------------------------------------------------------------------------------------------------------------------------------------------------------

function spam_log(message, level)
	if not level then level = DIR.log_level.debug end
	if DIR.log[level] then log("DIR2: "..DIR.log_prefix[level]..message) end
end

------------------------------------------------------------------------------------------------------------------------------------------------------
