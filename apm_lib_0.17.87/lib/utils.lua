if apm.lib == nil then apm.lib = {} end
if apm.lib.utils == nil then apm.lib.utils = {} end

if not apm.lib.utils.constants then apm.lib.utils.constants = {} end

if not apm.lib.utils.data then apm.lib.utils.data = {} end
if not apm.lib.utils.data.tables then apm.lib.utils.data.tables = {} end

if not apm.lib.utils.math then apm.lib.utils.math = {} end
require 'lib.utils.math'

if not apm.lib.utils.string then apm.lib.utils.string = {} end
require 'lib.utils.string'

if not apm.lib.utils.setting then apm.lib.utils.setting = {} end
require 'lib.utils.setting'

if not apm.lib.utils.autoplace_controls then apm.lib.utils.autoplace_controls = {} end
require 'lib.utils.autoplace_controls'

if not apm.lib.utils.icon then apm.lib.utils.icon = {} end
require 'lib.utils.icon'

if not apm.lib.utils.pipecovers then apm.lib.utils.pipecovers = {} end
require 'lib.utils.pipecovers'

if not apm.lib.utils.category then apm.lib.utils.category = {} end
require 'lib.utils.category'

if not apm.lib.utils.description then apm.lib.utils.description = {} end
require 'lib.utils.description'

if not apm.lib.utils.character then apm.lib.utils.character = {} end
require 'lib.utils.character'

if not apm.lib.utils.item then apm.lib.utils.item = {} end
require 'lib.utils.item'

if not apm.lib.utils.fluid then apm.lib.utils.fluid = {} end
require 'lib.utils.fluid'

if not apm.lib.utils.barrel then apm.lib.utils.barrel = {} end
require 'lib.utils.barrel'

if not apm.lib.utils.boiler then apm.lib.utils.boiler = {} end
require 'lib.utils.boiler'

if not apm.lib.utils.generator then apm.lib.utils.generator = {} end
require 'lib.utils.generator'

if not apm.lib.utils.furnace then apm.lib.utils.furnace = {} end
require 'lib.utils.furnace'

if not apm.lib.utils.inserter then apm.lib.utils.inserter = {} end
require 'lib.utils.inserter'

if not apm.lib.utils.mining_drill then apm.lib.utils.mining_drill = {} end
require 'lib.utils.mining_drill'

if not apm.lib.utils.assembler then apm.lib.utils.assembler = {} end
require 'lib.utils.assembler'

if not apm.lib.utils.reactor then apm.lib.utils.reactor = {} end
require 'lib.utils.reactor'

if not apm.lib.utils.lab then apm.lib.utils.lab = {} end
require 'lib.utils.lab'

if not apm.lib.utils.car then apm.lib.utils.car = {} end
require 'lib.utils.car'

if not apm.lib.utils.locomotive then apm.lib.utils.locomotive = {} end
require 'lib.utils.locomotive'

if not apm.lib.utils.turret then apm.lib.utils.turret = {} end
require 'lib.utils.turret'

if not apm.lib.utils.bot then apm.lib.utils.bot = {} end
require 'lib.utils.bot'

if not apm.lib.utils.fuel then apm.lib.utils.fuel = {} end
require 'lib.utils.fuel'

if not apm.lib.utils.recipe then apm.lib.utils.recipe = {} end
require 'lib.utils.recipe'

if not apm.lib.utils.resource then apm.lib.utils.resource = {} end
require 'lib.utils.resource'

if not apm.lib.utils.modules then apm.lib.utils.modules = {} end
require 'lib.utils.modules'

if not apm.lib.utils.technology then apm.lib.utils.technology = {} end
require 'lib.utils.technology'

if not apm.lib.utils.recycling then apm.lib.utils.recycling = {} end
require 'lib.utils.recycling'

if not apm.lib.utils.crate_packing then apm.lib.utils.crate_packing = {} end
require 'lib.utils.crate_packing'

if not apm.lib.utils.starfall then apm.lib.utils.starfall = {} end
require 'lib.utils.starfall'

if not apm.lib.utils.tile then apm.lib.utils.tile = {} end
require 'lib.utils.tile'

if not apm.lib.utils.builder then apm.lib.utils.builder = {} end
require 'lib.utils.builder'

if not apm.lib.utils.patch then apm.lib.utils.patch = {} end
require 'lib.utils.patch.aai'
require 'lib.utils.patch.space_exploration'